public class Solution {
    public int singleNumber(int[] A) {
        if(A.length == 0 || A == null){
            return -1;
        }
        int result = 0;
        for (int i = 0; i < A.length; i++){
            result ^= A[i];
        }
     return result;   
    }
}
